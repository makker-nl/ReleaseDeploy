# SQL cl
Since about Oracle DB 12c, Oracle delivers sqlcl. I used to ship it with the Release Deploy framework.
But lets not do that, anymore.

ToDo: add a property that points to the Oracle Home location and/or sqlcl home folder, and copy it from there, instead of putting binaries and software that I don't own on GitHub.
(Always bad Idea to have binaries and/or non-sourcecode files in a git repo.)